{
  "name": "tally",
  "version": "0.0.0",
  "private": true,
  "packageManager": "pnpm@10.0.0",
  "scripts": {
    "test:pipe": "vitest run tests/bridge",
    "test:report": "rm -rf test-results && vitest run tests/bridge --reporter=default --reporter=json --reporter=html --outputFile.json=test-results/results.json --outputFile.html=test-results/html/index.html && node scripts/write-test-status.mjs",
    "test": "pnpm test:pipe"
  },
  "devDependencies": {
    "@vitest/ui": "2.1.9",
    "typescript": "^5.6.3",
    "vitest": "^2.1.8"
  }
}
